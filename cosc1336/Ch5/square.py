# square area takes one argument (side_length) and returns the side_length, squared
def square_area(side_length):
    return side_length ** 2

# square perimeter takes one argument (side_length) and returns the side_length multiplied by 4
def square_perimeter(side_length):
    return side_length * 4